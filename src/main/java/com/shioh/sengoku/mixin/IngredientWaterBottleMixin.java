package com.shioh.sengoku.mixin;

// This file was originally intended to provide a mixin to accept water bottles in
// crafting recipes. That functionality is now provided by a recipe JSON change
// (the recipe accepts either a water bucket or a water potion entry), so the
// mixin was disabled. Keep a minimal marker class here to avoid compilation
// problems while keeping history in the workspace.
public final class IngredientWaterBottleMixin {
    private IngredientWaterBottleMixin() {}
}
