package com.shioh.sengoku.mixin;

/**
 * Disabled legacy combined TrapDoor mixin.
 * Replaced by TrapDoorStiffStateMixin and BlockUseTrapdoorStiffMixin.
 */
public final class TrapDoorStiffMixin {
    // Intentionally empty: keeps older references from interfering while new split mixins are used.
}
