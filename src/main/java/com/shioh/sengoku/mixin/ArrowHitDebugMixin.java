package com.shioh.sengoku.mixin;

/**
 * Placeholder for removed ArrowHitDebugMixin.
 * The temporary debug mixin caused runtime mixin injection failures and has been disabled.
 */
public final class ArrowHitDebugMixin {
    // Intentionally empty — debug mixin removed to avoid mixin/apply failures at runtime.
}
