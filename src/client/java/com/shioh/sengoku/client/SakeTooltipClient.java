package com.shioh.sengoku.client;

import net.fabricmc.api.ClientModInitializer;

public class SakeTooltipClient implements ClientModInitializer {
    @Override
    public void onInitializeClient() {
        // Client-side fermented sake tooltip removed — server-side tooltip is used instead.
    }
}
