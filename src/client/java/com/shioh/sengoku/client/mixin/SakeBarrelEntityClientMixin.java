package com.shioh.sengoku.client.mixin;

// Client mixin previously used to toggle a tooltip flag; removed behavior.
public class SakeBarrelEntityClientMixin {
    // Intentionally left blank — fermented sake tooltip is handled server-side.
}
