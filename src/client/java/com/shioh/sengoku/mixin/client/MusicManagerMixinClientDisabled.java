package com.shioh.sengoku.mixin.client;

// Disabled placeholder to avoid duplicate class name after adding reflective mixin in main sources.
public class MusicManagerMixinClientDisabled {
    // intentionally empty
}
